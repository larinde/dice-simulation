pipeline {
    agent any

    stages {

        stage("build"){
            steps{
                echo 'building project'

            }
        }

        stage("test"){
            echo 'running test cases...'
        }

        stage("deploy"){
            echo 'deploying artifact...'
        }
    }
}